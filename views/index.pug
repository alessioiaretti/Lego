doctype html
html
 head
   title #{title}
   link(rel='stylesheet', href='/css/style.css')
   meta(name="viewport" content="width=device-width, initial-scale=1")
 body
   header.header
    h1 #{title}
   article.container
     each elemento in scatole
       div.elemento
         div.elemento-image(style=`background: url('/images/${elemento.imgSrc}') top center no-repeat; background-size: cover;`)
         h2.elemento-name
           | #{elemento.name} 
         a(href=`/istruzioni?nome=${elemento.name}`)
           | Visualizza istruzioni
           


